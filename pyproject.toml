[project]
name = "miniblockchain"
version = "0.1.0"
description = "A modest blockchain implementation from scratch."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.2",
    "uvicorn>=0.42.0",
]
